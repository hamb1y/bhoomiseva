---
import EntryIndex from "./EntryIndex.astro";
import type { Lang } from "../i18n/ui";
import { useTranslations, localePath, lx } from "../i18n/utils";
import { stories } from "../data/stories";
import { programs } from "../data/programs";
import { ArrowUpRight } from "lucide-astro";

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = useTranslations(lang);
const programOptions = programs.map((p) => ({ value: p.id, label: lx(p.kicker, lang) }));
---

<EntryIndex
  lang={lang}
  path="/stories"
  basePath="/stories"
  entries={stories}
  kicker={t("stories.kicker")}
  title={t("stories.title")}
  lede={t("stories.lede")}
  pageTitle={t("nav.stories")}
  emptyMessage={t("stories.empty")}
  programs={programOptions}
  showProgram
  showYear
>
  <section class="events" slot="after">
    <div class="container">
      <div class="events-cta">
        <div>
          <h2 class="events-title">{t("events.title")}</h2>
          <p>{t("events.lede")}</p>
        </div>
        <a class="btn btn--outline" href={localePath("/events", lang)}>
          {t("cta.allEvents")}
          <ArrowUpRight size={16} stroke-width={1.7} aria-hidden="true" />
        </a>
      </div>
    </div>
  </section>
</EntryIndex>

<style>
  .events .container {
    border-top: 1px solid var(--rule);
    padding-top: var(--s-6);
  }
  .events-cta {
    display: flex;
    flex-wrap: wrap;
    align-items: end;
    justify-content: space-between;
    gap: var(--s-5);
  }
  .events-title {
    font-size: var(--step-2);
    margin: 0 0 var(--s-3);
  }
  .events-cta p {
    margin: 0;
    color: var(--ink-2);
    max-width: 52ch;
  }
</style>
